using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MasterTurn : MonoBehaviour
{
	public int t;
	public static int turn;
    // Start is called before the first frame update
    void Start()
    {
        turn = t;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
